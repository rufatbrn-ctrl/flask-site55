from flask import Flask, request
import requests
import os

app = Flask(__name__)

# ===== НАЛАШТУВАННЯ =====
8413617310:AAG3tORFs6Rq-9JX6t0A3EFPuEVDDvax06s = os.getenv("TG_TOKEN")      # токен бота
-1003637372928 = os.getenv("TG_CHAT_ID")      # id групи або адміна

# ===== ГОЛОВНА СТОРІНКА САЙТУ =====
@app.route("/")
def home():
    return """
    <h1>Сайт працює 24/7 ✅</h1>
    <form action="/send" method="post">
        <input name="text" placeholder="Написати в Telegram">
        <button type="submit">Надіслати</button>
    </form>
    """

# ===== ВІДПРАВКА В TELEGRAM =====
@app.route("/send", methods=["POST"])
def send():
    text = request.form.get("text")

    if not text:
        return "Порожнє повідомлення"

    url = f"https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN}/sendMessage"
    data = {
        "chat_id": TELEGRAM_CHAT_ID,
        "text": f"🌐 З сайту:\n{text}"
    }

    requests.post(url, data=data)
    return "Відправлено в Telegram ✅"

# ===== ЗАПУСК =====
if __name__ == "__main__":
    app.run(host="0.0.0.0", port=10000)
